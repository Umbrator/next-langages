/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['images.unsplash.com', 'cdn-icons-png.flaticon.com' , 'www.upload.ee'],
    },
  };
  
  export default nextConfig;
  